#!/usr/bin/env ruby
require "rubygems"
require "bundler/setup"

# Same result-affecting flags as the lint job in .github/workflows/ruby.yml
# (-A -q -w2); CI adds --no-pager. -w2 filters Weak-confidence findings only.
ARGV.unshift("-A", "-q", "-w2")

load Gem.bin_path("brakeman", "brakeman")
